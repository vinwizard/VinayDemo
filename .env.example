# Optional. The demo needs NO keys. The live adapter is not implemented/tested yet;
# setting a key only changes the status text until providers/live.py is completed.
# Never commit a real .env (it is gitignored). Secrets are never written to runs or exports.
GEMINI_API_KEY=
LIVE_MODEL=
